# Supabase project URL (Settings → API → Project URL)
NEXT_PUBLIC_SUPABASE_URL=https://sqxcbmgdnnzxgamjwlzz.supabase.co

# Publishable or anon key (Settings → API → Publishable key / anon key)
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only — for admin scripts and migrations (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=
